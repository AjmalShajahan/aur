diff --git a/bottles/backend/managers/manager.py b/bottles/backend/managers/manager.py
index 4d320ed..4589805 100644
--- a/bottles/backend/managers/manager.py
+++ b/bottles/backend/managers/manager.py
@@ -1323,6 +1323,7 @@ class Manager(metaclass=Singleton):
                     "pulseaudio_latency": _program.get("pulseaudio_latency"),
                     "virtual_desktop": _program.get("virtual_desktop"),
                     "winebridge": _program.get("winebridge"),
+                    "wayland": _program.get("wayland"),
                     "hide_console": _program.get("hide_console"),
                     "autostart": _program.get("autostart", False),
                     "file_extensions": _program.get("file_extensions", []),
diff --git a/bottles/backend/managers/steam.py b/bottles/backend/managers/steam.py
index 75935bf..1f42ef1 100644
--- a/bottles/backend/managers/steam.py
+++ b/bottles/backend/managers/steam.py
@@ -99,11 +99,22 @@ class SteamManager:
             )
 
         return max(
-            (path for path in self.__get_steam_paths() if os.path.isdir(path)),
+            (
+                path
+                for path in self.__get_steam_paths()
+                if self.__is_usable_steam_path(path)
+            ),
             key=steam_data_score,
             default=None,
         )
 
+    @staticmethod
+    def __is_usable_steam_path(path: str) -> bool:
+        """Return whether a Steam root contains the metadata we need."""
+        library_folders = os.path.join(path, "steamapps/libraryfolders.vdf")
+        local_configs = glob(os.path.join(path, "userdata/*/config/localconfig.vdf"))
+        return os.path.isfile(library_folders) and bool(local_configs)
+
     def __get_scoped_path(self, scope: str = "steamapps"):
         """scopes: steamapps, userdata"""
         if scope not in ["steamapps", "userdata"]:
diff --git a/bottles/backend/wine/executor.py b/bottles/backend/wine/executor.py
index fd3d238..18c28a0 100644
--- a/bottles/backend/wine/executor.py
+++ b/bottles/backend/wine/executor.py
@@ -93,6 +93,7 @@ class WineExecutor:
         program_gamescope: Optional[bool] = None,
         program_virt_desktop: Optional[bool] = None,
         program_winebridge: Optional[bool] = None,
+        program_wayland: Optional[bool] = None,
         program_hide_console: bool = False,
         sandbox_override: Optional[str] = None,
     ):
@@ -176,6 +177,14 @@ class WineExecutor:
         ):
             self.environment["GAMESCOPE"] = "1" if program_gamescope else "0"
 
+        if program_wayland is not None:
+            self.environment["BOTTLES_WAYLAND"] = "1" if program_wayland else "0"
+            if program_wayland != getattr(self.config.Parameters, "wayland", False):
+                # WineBridge is a long-lived process started with the bottle-wide
+                # display driver. Launch directly so this process can use its
+                # per-program display environment.
+                self.use_winebridge = False
+
         if env_dll_overrides:
             if "WINEDLLOVERRIDES" in self.environment:
                 self.environment["WINEDLLOVERRIDES"] += ";" + ";".join(
@@ -272,6 +281,7 @@ class WineExecutor:
             program_gamescope=program.get("gamescope"),
             program_virt_desktop=program.get("virtual_desktop"),
             program_winebridge=program.get("winebridge"),
+            program_wayland=program.get("wayland"),
             program_hide_console=program.get("hide_console") is True,
             sandbox_override=sandbox_override,
         )
diff --git a/bottles/backend/wine/winecommand.py b/bottles/backend/wine/winecommand.py
index 38f32f2..9faaf35 100644
--- a/bottles/backend/wine/winecommand.py
+++ b/bottles/backend/wine/winecommand.py
@@ -131,13 +131,29 @@ def _wayland_available(env: "WineEnv") -> bool:
 
 
 def apply_wayland_preferences(
-    env: "WineEnv", params, xlocale_dir: Optional[str] = None
+    env: "WineEnv",
+    params,
+    xlocale_dir: Optional[str] = None,
+    wayland_override: Optional[bool] = None,
 ) -> None:
     proton_wayland_enabled = _proton_option_enabled(env.get_value, "WAYLAND")
-    if not _wayland_requested(env, params):
+    wayland_activated = (
+        wayland_override
+        if wayland_override is not None
+        else _wayland_requested(env, params)
+    )
+    if not wayland_activated and wayland_override is None:
         return
     if DisplayUtils.display_server_type() != "wayland":
         return
+
+    if not wayland_activated:
+        env.remove("WAYLAND_DISPLAY")
+        display = os.environ.get("DISPLAY")
+        if not env.has("DISPLAY") and display:
+            env.add("DISPLAY", display, override=True)
+        return
+
     wayland_display = os.environ.get("WAYLAND_DISPLAY")
     if not env.has("WAYLAND_DISPLAY") and wayland_display:
         env.add("WAYLAND_DISPLAY", wayland_display, override=True)
@@ -473,6 +489,8 @@ class WineCommand:
         if environment is None:
             environment = {}
 
+        wayland_override = environment.pop("BOTTLES_WAYLAND", None)
+
         bottle = ManagerUtils.get_bottle_path(config)
         runner_path = ManagerUtils.get_runner_path(config.Runner)
         proton_path = ""
@@ -768,7 +786,14 @@ class WineCommand:
             candidate = os.path.join(runner_path, "share/X11/locale")
             if os.path.isdir(candidate):
                 xlocale_dir = candidate
-        apply_wayland_preferences(env, params, xlocale_dir)
+        apply_wayland_preferences(
+            env,
+            params,
+            xlocale_dir,
+            wayland_override=(wayland_override == "1")
+            if wayland_override is not None
+            else None,
+        )
         apply_hdr_preferences(
             env,
             params,
diff --git a/bottles/frontend/ui/dialog-launch-options.blp b/bottles/frontend/ui/dialog-launch-options.blp
index 9c48a0c..322a216 100644
--- a/bottles/frontend/ui/dialog-launch-options.blp
+++ b/bottles/frontend/ui/dialog-launch-options.blp
@@ -276,6 +276,15 @@ template $LaunchOptionsDialog: Adw.Window {
             valign: center;
           }
         }
+
+        Adw.ActionRow action_wayland {
+          title: _("Wayland (Experimental)");
+          activatable-widget: switch_wayland;
+
+          Switch switch_wayland {
+            valign: center;
+          }
+        }
       }
     }
   }
diff --git a/bottles/frontend/windows/launchoptions.py b/bottles/frontend/windows/launchoptions.py
index a18c701..30a6824 100644
--- a/bottles/frontend/windows/launchoptions.py
+++ b/bottles/frontend/windows/launchoptions.py
@@ -58,6 +58,7 @@ class LaunchOptionsDialog(Adw.Window):
     switch_winebridge = Gtk.Template.Child()
     switch_gamescope = Gtk.Template.Child()
     switch_virt_desktop = Gtk.Template.Child()
+    switch_wayland = Gtk.Template.Child()
     action_d7vk = Gtk.Template.Child()
     action_dxvk = Gtk.Template.Child()
     action_vkd3d = Gtk.Template.Child()
@@ -66,6 +67,7 @@ class LaunchOptionsDialog(Adw.Window):
     action_gamescope = Gtk.Template.Child()
     action_cwd = Gtk.Template.Child()
     action_virt_desktop = Gtk.Template.Child()
+    action_wayland = Gtk.Template.Child()
     action_automatic_backup = Gtk.Template.Child()
     btn_automatic_backup = Gtk.Template.Child()
     # endregion
@@ -133,6 +135,7 @@ class LaunchOptionsDialog(Adw.Window):
         self.toggled["gamescope"] = False
         self.toggled["virtual_desktop"] = False
         self.toggled["winebridge"] = False
+        self.toggled["wayland"] = False
 
         # connect signals
         self.btn_save.connect("clicked", self.__save)
@@ -159,6 +162,9 @@ class LaunchOptionsDialog(Adw.Window):
         self.global_winebridge = program_winebridge = getattr(
             config.Parameters, "winebridge", True
         )
+        self.global_wayland = program_wayland = getattr(
+            config.Parameters, "wayland", False
+        )
 
         if self.program.get("d7vk") is not None:
             program_d7vk = self.program.get("d7vk")
@@ -181,6 +187,9 @@ class LaunchOptionsDialog(Adw.Window):
         if self.program.get("winebridge") is not None:
             program_winebridge = self.program.get("winebridge")
             self.action_winebridge.set_subtitle(self.__msg_override)
+        if self.program.get("wayland") is not None:
+            program_wayland = self.program.get("wayland")
+            self.action_wayland.set_subtitle(self.__msg_override)
 
         self.switch_d7vk.set_active(program_d7vk)
         self.switch_dxvk.set_active(program_dxvk)
@@ -189,6 +198,7 @@ class LaunchOptionsDialog(Adw.Window):
         self.switch_gamescope.set_active(program_gamescope)
         self.switch_virt_desktop.set_active(program_virt_desktop)
         self.switch_winebridge.set_active(program_winebridge)
+        self.switch_wayland.set_active(program_wayland)
 
         self.switch_d7vk.connect(
             "state-set", self.__check_override, self.action_d7vk, "d7vk"
@@ -214,6 +224,9 @@ class LaunchOptionsDialog(Adw.Window):
         self.switch_winebridge.connect(
             "state-set", self.__check_override, self.action_winebridge, "winebridge"
         )
+        self.switch_wayland.connect(
+            "state-set", self.__check_override, self.action_wayland, "wayland"
+        )
 
         if program.get("pre_script") not in ("", None):
             self.action_pre_script.set_subtitle(program["pre_script"])
@@ -263,6 +276,7 @@ class LaunchOptionsDialog(Adw.Window):
         program_gamescope = self.switch_gamescope.get_state()
         program_virt_desktop = self.switch_virt_desktop.get_state()
         program_winebridge = self.switch_winebridge.get_state()
+        program_wayland = self.switch_wayland.get_state()
 
         self.__set_override("d7vk", program_d7vk, self.global_d7vk)
         self.__set_override("dxvk", program_dxvk, self.global_dxvk)
@@ -273,6 +287,7 @@ class LaunchOptionsDialog(Adw.Window):
             "virtual_desktop", program_virt_desktop, self.global_virt_desktop
         )
         self.__set_override("winebridge", program_winebridge, self.global_winebridge)
+        self.__set_override("wayland", program_wayland, self.global_wayland)
         self.program["arguments"] = self.entry_arguments.get_text()
         self.program["arguments_enabled"] = self.switch_arguments.get_active()
         if self.switch_hide_console.get_active():
@@ -486,6 +501,7 @@ class LaunchOptionsDialog(Adw.Window):
         self.switch_gamescope.set_active(self.global_gamescope)
         self.switch_virt_desktop.set_active(self.global_virt_desktop)
         self.switch_winebridge.set_active(self.global_winebridge)
+        self.switch_wayland.set_active(self.global_wayland)
         self.action_d7vk.set_subtitle("")
         self.action_dxvk.set_subtitle("")
         self.action_vkd3d.set_subtitle("")
@@ -493,6 +509,7 @@ class LaunchOptionsDialog(Adw.Window):
         self.action_gamescope.set_subtitle("")
         self.action_virt_desktop.set_subtitle("")
         self.action_winebridge.set_subtitle("")
+        self.action_wayland.set_subtitle("")
         self.__set_disabled_switches()
         for name in self.toggled:
             self.toggled[name] = None
